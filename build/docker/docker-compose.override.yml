version: '3.4'
services:
  ########################
  # SERVICE DEPENDENCIES #
  ########################
  zuju-test:
    image: ${IMAGE_NAME}:dev
  zuju-test-migrator:
    image: ${IMAGE_NAME}:dev

networks:
  default:
